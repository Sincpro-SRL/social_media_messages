<odoo>
    <!--
    Para añadir un boton usando el mismo metodo ese necesario crear un record o un template como se muestra en la sigueinte linea:
    <record id="name_of_the_record" model="ir.ui.view">
        <field name="name">name.of.the.record</field>
        Tambien es importante colocar el modelo al cual se va a heredar la vista:
        <field name="model">modelo.a.heredar</field>
        Despues es importante especificar la view o el formulario al cual se le añadira el boton:
        <field name="inherit_id" ref="vista.a.heredar" />
        <field name="arch" type="xml">
            Para poder colocar el boton en el lugar que se desea es necesario especificarlo con el atributo "xpath" y "position" para el ejemplo en position
            se asigna "after" para indicar que se añadira el boton despues de la posicion descrita en el xpath:
            <xpath expr="//posicion//del//xml[@name='nombre_del_field']" position="after">
                Para anadir el boton es necesario especificar tres atributos el "type", "name", "string"
                Para este caso el type se debera asignar "object"
                Para name debe asignarce el metodo que esta definido en el modelo heredado
                Para string se debe asignar una cadena que sera mostrada en la interfax de odoo
                <button type="object" name="traceability_whatsapp" string="Enviar por Whatsapp" />
            </xpath>
        </field>
    </record>
    -->
    <record id="view_move_form_whatsapp_button" model="ir.ui.view">
        <field name="name">view.move.form.whatsapp.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//form//header//button[@name='preview_invoice']" position="after">
                <button type="object" name="traceability_whatsapp" string="Enviar por Whatsapp" />
            </xpath>
        </field>
    </record>
</odoo>